import React from 'react';
import { useParams } from "react-router-dom";
const Buy = () => {
    const params = useParams();
    return (
        <div>
           <h1>Buy</h1> 
        </div>
    );
};

export default Buy;